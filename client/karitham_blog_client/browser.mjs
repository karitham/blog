import {
  fetch_text,
  set_inner_html,
  set_attribute,
  remove_attribute,
  log_error,
  set_interval,
  is_visible,
  on_visibility_change,
} from "./browser_ffi.mjs";

export {
  fetch_text,
  is_visible,
  log_error,
  on_visibility_change,
  remove_attribute,
  set_attribute,
  set_inner_html,
  set_interval,
};
